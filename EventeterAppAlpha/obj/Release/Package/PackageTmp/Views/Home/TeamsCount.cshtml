@model IEnumerable<EventeterAppAlpha.Models.GetCountTeams_Result>
@{
    ViewBag.Title = "TeamsCount";
}
<button type="button" class="btn btn-primary" value="Home" onclick="location.href='@Url.Action("Index", "Home")'">
    Home
</button>
<h2>TeamsCount</h2>

<div class="panel panel-default">
    <div class="panel-heading"> Current total number of existing teams, come join the fun!</div>
    <div class="panel-body">
        <table>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.First().NumberOfTeams)

                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(model => item.NumberOfTeams)
                    </td>
                </tr>
            }
        </table>
    </div>
</div>