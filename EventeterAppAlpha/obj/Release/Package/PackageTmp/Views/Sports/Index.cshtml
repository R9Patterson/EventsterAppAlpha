@model IEnumerable<EventeterAppAlpha.Models.Sport>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SportName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location.Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SportName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location.Address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SportID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SportID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SportID })
        </td>
    </tr>
}

</table>
